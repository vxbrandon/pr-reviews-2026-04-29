# PR #668 — Backend-Server `dev-coverage-and-odds-impro` Review

> Branch `dev-coverage-and-odds-impro` → base `dev`. Author @vxbrandon. Opened 2026-04-29.
> Hierarchical 3-level review with **flow-correctness priority** (per latest user direction).
> Mid-level: [`_pr-668-flows.md`](_pr-668-flows.md), [`_pr-668-arch.md`](_pr-668-arch.md), [`_pr-668-tests.md`](_pr-668-tests.md), [`_pr-668-risk.md`](_pr-668-risk.md).
> Leaves under [`pr-668-leaves/`](pr-668-leaves/) (17 files; flows branch is the deepest with 7 leaves including 3 deep walks).

---

## 0. TL;DR — Flow-correctness picture

**Recommendation: REQUEST CHANGES.** PR #668 ships substantial value; the data flows themselves are mostly correct. The flow-correctness gaps are concentrated on observability and request-correlation: `request_id` and `X-Flow` are NOT propagated on the BS→BO admin call; 5 admin routes are missing from the flow registry; admin daemon threads lose ContextVars; the scheduler service still imports an old `logging_formatter` path. None are correctness-fatal but they undermine the structured-logging story the rest of the codebase relies on.

| Headline | Value |
|---|---|
| Commits ahead of `dev` | 4 |
| Files changed | 426 (+89,277 / −10,784) |
| HTTP routes added/changed | 45 (40 new, 5 changed) |
| Scheduled jobs added/changed | 2 net-new (`prelive_lineups_imminent` 90s, `sf_enrich --tier`) + 5 reorganised |
| Kafka emit sites added | 2 (`BetPlacedEvent` from UserBetService; `OddsRequestEvent` from OddsClient) |
| Cross-service HTTP surfaces added | 2 (`BO /live/is_live_batch`, `BO /odds/admin/migration/run-targeted`) |
| **WebSocket flows added** | **0** |
| Migrations added | 22 (16 net-new tables; 1 destructive: `20260420_dedup_competitions`) |
| Tests added | 62 files / ~10,705 LOC |
| **Hard blockers** | **5** (all flow-correctness or observability except dedup migration) |
| **Soft blockers** | **7** |

The bundle: SF coverage v1 (#652) + odds-microservice latency fixes (#598/#599/#628) + Kafka producer (#603).

## 0.1 Theme tree

```
PR #668
 ├── ① SofaScore coverage v1
 │     ├── 17+ sf_* tables + cross-table FKs
 │     ├── domain/identity/ (NEW package)
 │     ├── application/services/sofascore/ + repositories/sofascore/
 │     ├── /info/sofascore/* (32 GET routes)
 │     ├── /admin/sofascore/{enrich,onboard,status} (3) + /admin/leagues/onboard* (5)
 │     ├── scraper/jobs/sf_enrich.py — tiered (live/post/daily/weekly/all)
 │     └── audit tables: scrape_runs, enrichment_runs, sf_enrichment_match_state, whoscored_runs, wh_player_resolution_audit
 │
 ├── ② Odds-microservice client latency fixes
 │     ├── DEFAULT_TIMEOUT 30s→5s, LIVE_STATUS_TIMEOUT 2s
 │     ├── get_live_status_batch (replaces N+1 fan-out)
 │     ├── GameInfoService._compute_live_status_map (partition + fallback)
 │     └── LIVE_CHECK_MODE / WINDOW config
 │
 ├── ③ BS → Backend-Odds back-channel
 │     ├── integrations/odds_service_client.py + _odds_admin_contracts.py
 │     ├── post-scrape sync from prelive_lineups, prelive_lineups_imminent, live_fetch
 │     ├── 90s prelive_lineups_imminent tier (NEW)
 │     ├── http vs subprocess modes; opt-in via ODDS_SERVICE_SYNC_ENABLED
 │     └── JWT-auth via _mint_admin_jwt (5min, role=admin)
 │
 ├── ④ Kafka / Redpanda producer
 │     ├── infrastructure/kafka/{config, producer, schemas}
 │     ├── app.on_event startup/shutdown lifecycle
 │     ├── 2 emit sites: OddsClient.get_parlay_odds + UserBetService (post-confirm)
 │     └── Topics: odds.user_bets, odds.odds_requests
 │
 ├── ⑤ Security / hardening (Priority 4 fixes)  ← deprioritized per user
 │     ├── SECURE_COOKIES on admin/cashier login + logout
 │     ├── /info/debug/teams_with_games → admin-guarded
 │     └── OTP / password-reset rate limiting
 │
 ├── ⑥ Critical bug fixes (Priority 1)
 │     ├── orchestrator_v3 dangling `count` reference
 │     ├── postmatch_refresh hardcoded season → SeasonNames.from_date()
 │     ├── transaction_service create_manual_withdrawal staticmethod fix
 │     └── BetNotificationTracking FK target rename
 │
 ├── ⑦ Notifications: WITHDRAWAL_COMPLETED removed
 │     └── Domain enum + handler removed; payment_metrics path retained
 │
 └── ⑧ Scheduler refactor
       ├── BACKEND_JOBS + SCRAPER_JOBS unified registry consumption
       ├── /admin/scheduler/run/{job_name} now supports `dry_run`
       └── JobSpec.supports_dry_run flag
```

---

## 1. Flows (priority section) — `_pr-668-flows.md`

> Specialist synthesis of 7 leaves (4 foundation + 3 deep walks):
> Foundation: [http-routes](pr-668-leaves/flows-http-routes.md) · [websocket](pr-668-leaves/flows-websocket.md) · [scheduled-kafka](pr-668-leaves/flows-scheduled-kafka.md) · [pipelines-workers](pr-668-leaves/flows-pipelines-workers.md).
> Deep walks: [deep-sf-data](pr-668-leaves/flows-deep-sf-data.md) · [deep-admin-jobs](pr-668-leaves/flows-deep-admin-jobs.md) · [deep-cross-service](pr-668-leaves/flows-deep-cross-service.md).

### 1.1 Inventory by surface

```
Mobile/web client ─┬─ HTTP /info/sofascore/* (32 GET, public)
                   ├─ HTTP /info/get_games_in_date_range (existing route, internal rewrite)
                   └─ HTTP /admin/* (admin-token gated)

APScheduler (separate ECS task) ─┬─ live_fetch (5min)
                                  ├─ prelive_lineups (5min, was 10min)
                                  ├─ prelive_lineups_imminent (90s NEW, 1h window)
                                  ├─ postmatch_quick/full
                                  ├─ metadata_fetch
                                  └─ sf_enrich --tier {live|post|daily|weekly|all} (NEW)

OddsClient.get_parlay_odds      → Kafka odds.odds_requests
UserBetService (post-confirm)   → Kafka odds.user_bets

Cross-service:
  BS scheduler → BO /odds/admin/migration/run-targeted   (opt-in, JWT auth, ❌ ContextVar headers)
  BS web      → BO /live/is_live_batch                   (request path, ✅ ContextVar headers)
  BS web      → BO /get_parlay_odds, /live/is_live, etc. (existing, ✅ ContextVar headers)
```

**WebSocket: none added or modified.** Existing `WebSocketMetricsMiddleware` is unmodified; Stream Chat WS is third-party client-direct. Cross-repo WS contract risk vs Backend-Odds is non-existent for this PR.

### 1.2 Public reads — `/info/sofascore/*` × 32 routes

[Full per-route walk in `flows-deep-sf-data.md`](pr-668-leaves/flows-deep-sf-data.md). Common spine:

```
client GET /info/sofascore/{group}/{id}[/{field}]
   ▼ get_sofascore_data_service (DI; infrastructure/api/dependencies.py)
SofascoreDataService.<method>
   ├─ id = await self.id_resolver.resolve_{match,player,team}(id)        ← UUID OR sf_*_id
   └─ return await self.repo.<method>(resolved_id, ...)
       ▼ AsyncSession query against sf_match_*, sf_player_*, sf_team_*, players, teams
       ▼ For *_image / *_media: S3 signed URL fallback to SofaScore CDN
```

Grouping: 17 single-match field routes + 5 player + 7 team + 3 league + 2 odds + 2 standings/top-players + 2 ID-resolution.

| Aspect | Status |
|---|---|
| `flow_context.ROUTE_TO_FLOW` covers all 32 | ✅ `data.sofascore` |
| `response_model=` declared | ❌ schemas exist but not wired |
| `@flow_traced` on service methods | ❌ none of ~22 methods |
| Auth dep | none (public reads) |
| Rate limit | none |

### 1.3 Admin job spawners — 4 routes

[Full walks in `flows-deep-admin-jobs.md`](pr-668-leaves/flows-deep-admin-jobs.md).

```
POST /admin/sofascore/enrich   →  Thread(_run_sf_enrich_sync, daemon=True)
                                       ▼ SfOrchestratorV4.run(...) → enrichment_audit
POST /admin/sofascore/onboard  →  Thread(_run_sf_onboard_sync, daemon=True)
                                       ▼ LeagueOnboarder.{link_one,link_all,discover,create,show_status}
POST /admin/leagues/onboard    →  start_onboard → Thread(_run_onboard_workflow, daemon=True)
                                       ▼ multi-stage state machine ending in SfOrchestratorV4
GET  /admin/sofascore/status   →  synchronous, no thread
                                       ▼ JOIN league_registry × tournament_schedule × sf_match_details (counts)
```

| Aspect | Status |
|---|---|
| Auth (parent router `verify_admin_token`) | ✅ |
| ContextVars propagated into daemon thread | ❌ — wrap with `contextvars.copy_context()` |
| `flow_context.ROUTE_TO_FLOW` for `/admin/sofascore/*` | ✅ `admin.scraper` (3) |
| `flow_context.ROUTE_TO_FLOW` for `/admin/leagues/onboard*` | ❌ **5 routes missing** |
| Job-status persistence | in-process `_jobs` dict (lost on uvicorn restart, per-worker) |

### 1.4 `/info/get_games_in_date_range` — N+1 → batch

```
GameInfoService._compute_live_status_map(games, now)         ← game_info_service.py:117-181
   ├─ partition: terminal status     → False
   ├─ partition: LIVE_CHECK_MODE=ALWAYS → True
   ├─ partition: kickoff is None     → False
   ├─ partition: outside [-WINDOW_BEFORE, WINDOW_AFTER]      → False
   └─ remaining → odds_client.get_live_status_batch([…])
                       │  timeout: LIVE_STATUS_TIMEOUT (2s)
                       │  headers: get_propagation_headers()  ← ✅ all 7 ContextVars
                       │
                       ├─ on success: dict[match_id → bool|None]
                       └─ on exception/None entry → time-based fallback (now > kickoff)
```

### 1.5 Cross-service — BS → BO targeted migration

[Full walk in `flows-deep-cross-service.md` §Y](pr-668-leaves/flows-deep-cross-service.md).

```
Scheduler tick → run_live_fetch (or prelive_lineups, prelive_lineups_imminent)
   └─ if not dry_run:
         sync_lineup_updates_to_odds_service(job_name)        ← integrations/odds_service_client.py
              ├─ if not ODDS_SERVICE_SYNC_ENABLED: return
              ├─ wh_game_ids = await _fetch_recently_scraped_wh_game_ids(window=15min)
              └─ branch on ODDS_SERVICE_SYNC_MODE
                    ├─ "http"  → POST {ODDS_SERVICE_URL}/odds/admin/migration/run-targeted
                    │                payload: TargetedMigrationRequest{game_ids, with_predicted_lineups, with_missing_players}
                    │                headers: _admin_auth_headers()           ← ❌ ONLY Authorization
                    │                timeout: 10s
                    │                → 202 {job_id, …}
                    │
                    └─ "subprocess"  → conda run python <script_in_BO>     [dev/test only]
```

#### 1.5.1 Header-propagation matrix — verified

| Caller | Authorization | request_id / flow / session | Verdict |
|---|---|---|---|
| `OddsClient.*` (request path) | n/a (in-VPC) | ✅ via `_propagation_headers` → `get_propagation_headers()` | ✅ |
| `_post_targeted_migration` (scheduler path) | ✅ JWT (5min, `role=admin`) | ❌ **NOT propagated** | ⚠️ |

The auth posture is correct. The **flow-correlation gap** is the issue: BO logs the targeted-migration request without `X-Request-ID`. One-line fix in `_admin_auth_headers()`.

### 1.6 Kafka producer — 2 emit sites

```
OddsClient.get_parlay_odds (existing route, post-response)
   └─ _emit_odds_request_event(endpoint="/get_parlay_odds", game_ids=[…], market_types=["parlay"])
            └─ producer.produce(topic="odds.odds_requests", key=user_id|"anonymous",
                                 value=OddsRequestEvent{...,session_id=request_id_ctx}.to_json())

UserBetService (post bet-confirm)
   └─ self._emit_bet_placed_event(bet)
            └─ producer.produce(topic="odds.user_bets", key=user_id,
                                 value=BetPlacedEvent(bet_id, user_id, game_ids, legs, stake, …).to_json())
```

Both fire-and-forget on the request thread (`producer.produce()` never raises). `OddsRequestEvent.session_id` ← `request_id_ctx` is captured before the request thread ends. ✅

### 1.7 Idempotency / scheduled-task safety

| Job | Idempotency | Lock |
|---|---|---|
| `live_fetch` (5 min) | ✅ ON CONFLICT DO UPDATE in BaseSink | `SingleInstanceLock` per job_id (process-local) |
| `prelive_lineups` (5 min) | ✅ same | same |
| `prelive_lineups_imminent` (90 s NEW) | ✅ same | process-local; cross-instance NOT safe |
| `postmatch_quick/full` | ✅ same | same |
| `metadata_fetch` | ✅ same | same |
| `sf_enrich` per tier | ✅ 24h gate at `sf_enrich.py:268-291` | same |
| `dq_audit`, `bet_audit` | unknown | same — pre-existing FIXME(task-5/6) broken imports |

Cross-instance: if APScheduler runs in 2+ scheduler ECS tasks, both fire concurrently. Sinks tolerate via ON CONFLICT. The BS→BO post-step relies on **BO** to dedupe `game_ids` server-side (cross-repo question).

### 1.8 Flow registry coverage gap

5 routes missing from `flow_context.ROUTE_TO_FLOW`:

```
POST   /admin/leagues/onboard
GET    /admin/leagues/onboard
GET    /admin/leagues/onboard/{job_id}
DELETE /admin/leagues/onboard/{job_id}
POST   /admin/leagues/onboard/preset/{preset_name}
```

Pre-commit drift hook fails. **HARD BLOCKER 1**.

### 1.9 Service-layer `@flow_traced` decoration gap

| Service | Decorated? |
|---|---|
| `services/sofascore/*.py` | ❌ none |
| `services/admin/*.py` | ❌ none |
| `services/statistics/game_info_service.py` (new method) | ❌ |
| `services/bets/user_bet_service.py` (existing) | ✅ multiple |
| `services/chat/chat_service.py` (existing) | ✅ multiple |

Middleware sets `flow_ctx` so log lines are tagged, but service-method-level OTEL spans are missing for the new routes. **SOFT BLOCKER 7**.

---

## 2. Architecture — `_pr-668-arch.md`

> Specialist synthesis of 4 leaves: [services](pr-668-leaves/arch-services.md) · [repositories](pr-668-leaves/arch-repositories.md) · [migrations](pr-668-leaves/arch-migrations.md) · [dtos-config](pr-668-leaves/arch-dtos-config.md).

### 2.1 New top-level namespaces

| Namespace | Purpose |
|---|---|
| `application/services/admin/` | Admin in-process job tracker + league-onboard service |
| `application/services/sofascore/` | Read API for `/info/sofascore/*` + ID resolver |
| `domain/identity/` | Cross-provider ID abstractions (WS ↔ SF ↔ internal UUIDs) |
| `infrastructure/integrations/` | Cross-service back-channel clients (BS → BO admin) |
| `infrastructure/kafka/` | Producer singleton, dataclass schemas, config |
| `infrastructure/repositories/sofascore/` | SF data repo |

### 2.2 Migrations (22 files; 1 destructive)

```
20260324_add_sofascore_tables_and_columns         ← 17+ tables + sf_* columns
20260413_add_remaining_sf_tables
20260414_{image_s3_key, national_team_to_player_profile}
20260416_add_sf_team_and_odds_tables
20260417_{fix_england_national_league_sf_tid, sf_player_404_cache}
20260420_dedup_competitions_add_unique            ← DESTRUCTIVE (affects flows §4 below)
20260423_{audit_tables, skipped_concurrent, sf_enrichment_match_state}
20260426_wh_player_resolution_audit
20260427_{add_missing_sf_fks, whoscored_runs_audit}     ← FKs must run AFTER dedup
20260428_add_config_to_audit_runs

+ 6 merge-head migrations
```

ORM-first compliance per `.claude/rules/orm-first.md` is satisfied — every previously flagged gap (#654, #656) is now closed in this PR.

### 2.3 Env / config

| Var(s) | In `.env.example`? | In `.required-env`? |
|---|---|---|
| `LIVE_CHECK_*` (3) | ✅ | ✅ |
| `KAFKA_*` (5) | implicit (defaults) | ✅ |
| `ODDS_SERVICE_SYNC_*` (8) | ✅ | **❌ — drift** |

`config/core/app.py`: `LiveCheckMode` enum, `LIVE_CHECK_MODE`, `LIVE_CHECK_WINDOW_BEFORE`, `LIVE_CHECK_WINDOW_AFTER`, `SECURE_COOKIES`.

Logging-formatter relocated to `core/utils/logging_formatter.py`. `app.py` and `middleware/request_id.py` updated; **`scheduler/main.py:34` NOT updated**.

### 2.4 Architectural notes

- Admin daemon-thread pattern + module-level `_jobs` dict diverges from the request-path async UoW pattern. Status lost on worker restart, per-worker.
- Kafka schemas are `@dataclass` (decoupled from web layer); producer fire-and-forget by design.
- Hand-mirrored TypedDict (`_odds_admin_contracts.py`) — calculated trade-off; promote to `specs/` when a 2nd consumer appears.
- 32 SF data routes lack `response_model=` despite schemas existing.

---

## 3. Tests — `_pr-668-tests.md`

> Specialist synthesis of 3 leaves: [unit](pr-668-leaves/tests-unit.md) · [integration](pr-668-leaves/tests-integration.md) · [coverage-gaps](pr-668-leaves/tests-coverage-gaps.md).

### 3.1 At a glance

62 new test files / ~10,705 LOC. SF unit tests (14 files / ~4,648 LOC) carry most of the coverage.

### 3.2 Implemented (high-value)

- ✅ SF data layer (resolver, schema drift, field mapping, season parser, name match, match-state).
- ✅ 4 named Priority-1 bug fixes (AST + behavioural).
- ✅ 3 named Priority-4 security fixes.
- ✅ `_compute_live_status_map` partition + fallback.
- ✅ Admin SF routes (auth, validation, 202 envelope).
- ✅ Layered shell-driven QA at `qa_test/odds-pricing-main/layer{1-6}/*.sh` for cross-service BS↔BO sync.

### 3.3 Top coverage gaps (flow-relevant)

1. **Kafka emit verification** — neither `_emit_*_event` site is unit-tested.
2. **`odds_service_client` end-to-end** — `_post_targeted_migration` HTTP path, `_fetch_recently_scraped_wh_game_ids` window-trim.
3. **`/admin/sofascore/onboard` non-STATUS actions** untested at route level.
4. **`response_model=` enforcement on `/info/sofascore/*`** — 32 routes, no contract guard.
5. **`/admin/leagues/onboard*` `ROUTE_TO_FLOW` drift test** — none in CI.
6. **`prelive_lineups_imminent` 90s tier** — argument shape, post-step hook untested.
7. **`20260420_dedup_competitions` migration upgrade-downgrade test** — destructive, no fixture test.
8. **Background-thread ContextVar propagation test** — silent observability gap.
9. **`_odds_admin_contracts` cross-repo drift detection** — manual only.

### 3.4 PR description checklist all blank — **HARD BLOCKER**.

---

## 4. Risks & blockers — `_pr-668-risk.md`

> Specialist synthesis of 4 leaves: [migrations](pr-668-leaves/risk-migrations.md) · [observability-env](pr-668-leaves/risk-observability-env.md) · [flow-correctness](pr-668-leaves/risk-flow-correctness.md) · [cross-repo](pr-668-leaves/risk-cross-repo.md).
>
> Note: per user direction, the previous `risk-auth-payment-concurrency` leaf was deprecated. Auth posture for the BS→BO admin call is correct (JWT minted with `role=admin`, 5-min lifetime).

### 4.1 Hard blockers (5)

| # | Blocker | Severity | Source |
|---|---|---|---|
| 1 | `/admin/leagues/onboard*` × 5 routes missing from `ROUTE_TO_FLOW` — drift hook fails | HIGH | flow §1.8, observability-env §A |
| 2 | `request_id` / `X-Flow` not propagated to BO on admin call (`_admin_auth_headers` returns only `Authorization`) | HIGH | flow §1.5.1, flow-correctness §A.4 |
| 3 | `src/scheduler/main.py:34` imports old `logging_formatter` path — scheduler may fail at startup | HIGH | observability-env §E |
| 4 | `20260420_dedup_competitions_add_unique` not smoke-tested on stage; companion `base_sink` follow-up not in this PR | HIGH | risk-migrations §B, flow-correctness §D.1 |
| 5 | PR description checklist all blank, including "Backtest with previous dev launch" | HARD | observability-env §H |

### 4.2 Soft blockers (7)

| # | Blocker | Severity |
|---|---|---|
| 6 | ContextVar loss in admin daemon threads (3 endpoints) — wrap with `contextvars.copy_context()` | MED |
| 7 | New SF / admin services NOT decorated with `@flow_traced(Flow.X)` — service-method spans missing | MED |
| 8 | `ODDS_SERVICE_SYNC_*` env vars in `.env.example` but missing from `.required-env` | MED |
| 9 | BO endpoint deploy ordering: `/live/is_live_batch` before BS deploy; `/odds/admin/migration/run-targeted` before flag flip; Kafka consumers before producer flag flip | MED |
| 10 | `WITHDRAWAL_COMPLETED` notif_outbox in-flight rows fall through to "Unknown event type" branch | MED |
| 11 | `_odds_admin_contracts.py` hand-mirrored TypedDict — drift detection manual | MED |
| 12 | BS does not dedupe `game_ids` across 90s ticks within 15min window — relies on BO idempotency (cross-repo question) | MED |

### 4.3 Operational notes (post-merge)

- Multi-instance scheduler scale-out unsafe for `SingleInstanceLock` (process-local).
- Admin job persistence — module-level `_jobs` dict lost on uvicorn restart, per-worker; cross-worker `GET .../jobs/{job_id}` may 404.
- `prelive_lineups_imminent` 90s × `ODDS_SERVICE_SYNC_ENABLED` → ~960 BS→BO requests/day. Verify BO load capacity. Add Grafana panel for `enrichment_runs.status='failed' WHERE tier='live'` (Slack disabled for this tier).
- `OddsClient.DEFAULT_TIMEOUT` 30→5s — non-`is_live` calls now error on slower BO without time-based fallback.
- Public `/info/sofascore/*` × 32 routes are unauthenticated and unrate-limited.
- 89k-line PR is hard to revert atomically; staged rollout: SF data routes → admin SF routes → BO sync flag → Kafka producer flag.

### 4.4 TODO/FIXME debt added

- `scraper_job_registry.py`: `TODO: when run_targeted_match_pipeline gains minutes-granularity filtering, tighten this to a 30-min window.`
- `scraper_job_registry.py`: two `FIXME(task-5/6)` for pre-existing broken `dq_audit` / `bet_audit` imports — explicitly NOT fixed.

### 4.5 Pre-existing risk surfaced (not introduced)

- `.env.example` contains real-looking Stream API secret + Slack tokens (committed in `643f376e`). Out of scope; should be rotated.

### 4.6 WebSocket protocol stability — non-issue

PR introduces zero WebSocket flows. No WS-related contract risk.

---

## 5. Suggested labels

Per `.claude/rules/pr-labels.md`:

- `needs-review` — base
- `ready-to-flow-test` — touches routes / services / migrations (this is the **real** Mac Mini trigger, not `flow-test`)
- `integration-tests` — adds flow tests
- `database` — substantial schema work
- `tier3` — full feature, large blast radius

Do NOT add `ready-to-stage` or `ready-to-prod` until the 5 hard blockers are resolved.

---

## 6. Final verdict

| Bucket | Status |
|---|---|
| **Flow correctness** | ⚠️ Data flows are correct, but observability / request-correlation has 3 specific gaps (5 ROUTE_TO_FLOW entries missing, header propagation gap on BS→BO admin, ContextVar loss in daemon threads). All mechanically small. |
| Architecture & DDD | ✅ New namespaces follow project layering. ORM-first satisfied. |
| Migrations | ⚠️ Single destructive migration (`20260420_dedup_competitions`); follow-up `base_sink` PR is a coordination requirement. |
| Cross-service | ⚠️ Hand-mirrored TypedDict + non-trivial deploy-order coupling with BO + 2 Kafka topics with no schema registry. |
| Tests | ✅ Substantial coverage; gaps concentrated in cross-service + Kafka emit verification. |
| Bug fixes | ✅ 4 named Priority-1 fixes with regression tests. |
| WebSocket | ✅ Non-issue (zero WS changes). |
| Documentation | ✅ Extensive in-tree docs; PR description checklist blank. |

**Disposition**: REQUEST CHANGES. The 5 hard blockers are mechanically small; once resolved (and the PR description is filled in with stage smoke + backtest evidence), the PR is ready for stage deployment with the operational guardrails in §4.3 applied.

The flow-correctness story is the user's primary concern, and its fix path is concrete:
1. 5 lines in `flow_context.ROUTE_TO_FLOW` for `/admin/leagues/onboard*`.
2. ~5 lines in `integrations/odds_service_client.py:_admin_auth_headers()` to merge `get_propagation_headers()`.
3. 1 line in `src/scheduler/main.py:34` to fix the import path.
4. 3-4 lines per admin endpoint to wrap thread spawn with `contextvars.copy_context()`.
5. 1 decorator per service method to add `@flow_traced(Flow.X)`.

Total: well under an hour of work plus the dedup-migration smoke-test on stage.
